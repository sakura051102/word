# ---------------------------------------------------------------------------
# 部署到 GitHub Pages 时不需要上传的东西
#
# 原则：仓库里只放【运行时真正要用的文件】+【重建脚本】，
#      不放原始数据。原始数据加起来 20 多 MB，
#      而它们的唯一用途是生成 data/wordbook.js —— 那个文件已经在仓库里了。
#
# ⚠ 代价说明：忽略了原始数据之后，从一份干净的 clone 是【重建不出】词库的。
#   原始数据请自己在本地留好（或另外找个地方备份），
#   仓库只是发布渠道，不是数据备份。
# ---------------------------------------------------------------------------

# 原始词表与详表（build-wordbook.js 的输入，共约 20 MB）
kaoyan_full_9602.jsonl
netem_full_list.json
KaoYan_2.json/
KaoYan_3.json/
*.zip

# 真题语料仓库（目前是个空 clone，工作区没 checkout 出来）
真题语料/

# docs/ —— 之前误把整个项目连同 .git 一起拷进 docs/ 当发布目录，
# 结果 git 把它记成「子模块指针（gitlink）」，里面的文件一个都没进仓库，
# 还导致 Pages 构建失败。真正的发布内容就在根目录，
# 这个拷贝已经没用，以后 git add . 也不会再捡到它。
docs/

# 个人备份文件，别手滑传上去
vocab-backup-*.json

# 系统垃圾
.DS_Store
Thumbs.db
desktop.ini
