# 部署到手机

目标：手机主屏上有个图标，点开全屏运行，**断网也能背**。

---

## 一、先在电脑上确认能跑

直接双击 `背单词.html` 就行。

本地 `file://` 打开时**没有** Service Worker（浏览器不允许），所以看不到离线效果，
但界面、特效、进度保存都正常。这一步只是确认没坏。

---

## 二、传到 GitHub Pages

### 1. 建仓库并推上去

```bash
cd D:/desk/英语
git init
git add .
git commit -m "考研词汇 · 分级复习"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

`.gitignore` 已经把 20 多 MB 的原始词表挡在外面了，实际上传约 2.6 MB。

> ⚠ 仓库必须是 **Public**，否则免费账号开不了 Pages。
> 这里面没有任何隐私内容（你的学习进度存在浏览器里，不在仓库里），公开没问题。

### 2. 打开 Pages

仓库页面 → **Settings** → 左侧 **Pages** → Source 选 **Deploy from a branch**
→ 分支选 `main`、目录选 `/ (root)` → Save。

等一两分钟，页面顶部会给出网址：

```
https://你的用户名.github.io/仓库名/
```

访问根目录会被 `index.html` 自动转到 `背单词.html`。

---

## 三、装到手机主屏

**先用手机浏览器打开上面那个网址**，等页面完全加载（第一次要下 2.6 MB，走 WiFi）。

| 手机 | 操作 |
|---|---|
| Android / Chrome | 右上角 ⋮ → **添加到主屏幕** / **安装应用** |
| iPhone / Safari | 底部分享按钮 ↑ → **添加到主屏幕** |

> **iPhone 必须用 Safari。** 微信、QQ、UC 等内置浏览器不支持
> 「添加到主屏幕」，也不支持 Service Worker，装不了也离不了线。
> Android 上同理，用 Chrome 或 Edge，别用微信打开。

装好后从桌面图标进入就是全屏的，没有浏览器地址栏。**之后完全断网也能背**。

---

## 四、进度存在哪、怎么搬

### 存在哪

进度保存在**浏览器的 localStorage** 里，键名 `kaoyan_vocab_v1`。

隔离单位是 **origin（协议 + 域名 + 端口）**，不是文件：

```
file:///D:/desk/英语/背单词.html      ← 电脑上双击打开的
https://你的名字.github.io/仓库名/     ← 手机上装的
```

浏览器眼里这是**两个完全无关的网站，各存各的档**。

### 会保留的情况

- 同一台手机、同一个浏览器 → 一直在
- 关机、重启、几个月不打开 → 都在
- 清理浏览器**缓存** → 通常还在

### 会丢的情况

- 清理浏览器的 **Cookie 和网站数据**（不是"缓存"）→ **没了**
- 卸载浏览器 / 换手机 / 换浏览器 → **没了**
- iOS Safari 有个坑：**连续 7 天没打开过这个站点，系统会自动清掉存储**。
  装到主屏后这条限制会放宽，但也别长期晾着。

### 跨设备搬运

**没有自动同步**，电脑和手机是两份独立的档。要搬只能手动：

1. 旧设备：**设置 → 导出备份** → 得到 `vocab-backup-日期.json`
2. 用微信/网盘/邮件把文件传到新设备
3. 新设备：**设置 → 导入恢复** → 选那个文件

> **导入是完全覆盖**，不是合并。两台设备各背各的然后互相导入，
> 后导入的会把前面的整个盖掉。
>
> **所以：认准一台主力设备。** 建议手机当主力（随时能刷），
> 电脑只在想看统计图表时用，并且**每周导出一次备份**存网盘。
> 普查 5530 个词要好几个小时，丢一次真的很难受。

---

## 五、以后改了东西怎么更新

改完 push 上去就行，手机下次联网打开会自动拿到新版本。

Service Worker 用的是 stale-while-revalidate：先给缓存里的旧版本秒开，
同时后台拉新版存起来，**再下一次打开就是新的**。所以改完可能要开两次才见效。

**唯一的例外**：如果你重新生成了 `data/` 下的数据文件——

```bash
node tools/build-wordbook.js    # 重建词库 data/wordbook.js
node tools/build-corpus.js      # 重建真题原句索引 data/corpus.js
```

——必须手动把 `sw.js` 里的版本号加一：

```js
const VERSION = 'v2';   // ← 改成 'v3'
```

因为 `data/` 下那两个文件（词库 2.2 MB + 语料 452 KB）刻意**不做**后台重验
（每次打开都重下 2.6 MB 是在烧流量），只有版本号变了才会整个缓存重建。

---

## 六、开发时的自查

```bash
node tools/verify-wordbook.js       # 词库数据完整性 + 不变量检查
node tools/smoke-test.js            # 主流程冒烟测试（57 项）
node tools/smoke-test.js --reduced  # 系统开了「减少动态效果」时的路径
node tools/make-icons.js            # 重新生成 PWA 图标
node tools/build-corpus.js          # 重建真题原句索引
```

冒烟测试会把 9 个 js 真的加载起来跑完整流程（普查定级 → 熟词核对 → 回退
→ 复习翻卡 → 评分 → 选择题 → 连击），比 `node --check` 有用得多 ——
后者只能保证文件能被解析，保证不了"按 1 键能不能给单词定级"。

---

## 七、真题原句索引是怎么来的

卡片上的「真题原句」来自 `data/corpus.js`，由 `tools/build-corpus.js`
从 `真题语料/考研英语一阅读文本/`（1998–2022 年英语一阅读 + 翻译，147 篇）生成。

**语料本身没进仓库**（`.gitignore` 里排除了 `真题语料/`）——
它是个独立的 git 仓库，只是生成 `corpus.js` 的原料。要重新生成：

```bash
cd 真题语料
git fetch origin main:main && git checkout main
cd ..
node tools/build-corpus.js
```

### 一条必须记住的边界

**这是【词条级】索引，不是【义项级】。**

它只保证「这句话里有这个词」，**不保证句中用的是你正在看的那个义项**。

为什么不做义项级？因为那需要逐句做词义消歧，而**判错了比不标更误导人**——
把 `state` 标成「状态常考」而真题实际考的是「规定」，是在主动把你带偏。

好消息是词条级其实已经够用了。看 `account` 的三条原句：

```
accounted for less than 20% of international trade
accounted for under 70 percent of the total ton-miles
considered of no account in his society
```

一条「银行账户」都没有——**你自己一眼就看出考研考的是哪个义**，
这比一个我推测出来的标签可靠。`row` 同理，三条全是「排 / 连续」，没有「划船」。

### 覆盖率怎么看

总覆盖 3685 / 5530（66.6%），但**别看总数**，要看分频段：

| 词频段 | 覆盖率 |
|---|---|
| 真题 ≥200 次 | **99.5%** |
| 真题 50–199 次 | 92.6% |
| 真题 10–49 次 | 63.5% |
| 真题 1–9 次 | 25.4% |

高频词几乎全覆盖，低频词配不到原句很正常——这份语料只含**阅读和翻译**，
而词频统计是基于约 200 套完整真题（含完形、写作）的。

